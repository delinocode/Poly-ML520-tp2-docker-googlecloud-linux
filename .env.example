# Copy to a .env and set a value generated from:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
ML520_SECURITY__API_TOKEN=replace-me

# NOTE(TP): After having implemented the config, you will be able to set
#           configuration values with this. Test it out, but again,
#           configs belong in config files generally.

# env_prefix: ML520_
# If you want to dig into settings.serving.<attribute> you do: ML520_serving__attribute
# ML520_LOGGING__LEVEL=DEBUG
# ML520_SERVING__PREDICTION_THRESHOLD=0.35
